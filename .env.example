# Local development only. No provider integration is implemented yet.
API_HOST=127.0.0.1
API_PORT=3000
VITE_API_URL=http://127.0.0.1:3000

# Add provider credentials only after a provider is selected.
# Never commit real values.
# SPEECH_PROVIDER_API_KEY=
