{
  "name": "@project-bridge/evaluation",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:afriswitch:yoruba": "pnpm --filter @project-bridge/benchmark build && node scripts/prepare-afriswitch-yoruba.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@project-bridge/benchmark": "workspace:*",
    "@project-bridge/shared": "workspace:*"
  }
}
