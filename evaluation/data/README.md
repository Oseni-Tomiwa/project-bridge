# Local evaluation data

This directory is reserved for locally materialized external benchmark data. Its contents are ignored by Git except for this file.

Do not commit downloaded audio, Hugging Face caches, generated selection manifests, provider responses, or benchmark results. See `docs/datasets/afriswitch-yoruba.md` for the explicit preparation command and governance requirements.
